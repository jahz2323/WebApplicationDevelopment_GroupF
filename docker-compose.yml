services:
  django:
    image: webapp-image:latest
    container_name: webapp-container
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      DATABASE_URL: "sqlite:////db.sqlite3"
